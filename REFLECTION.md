# REFLECTION.md — 反思报告

## 哪些 Superpowers 技能发挥了最大作用

**Test-Driven Development（TDD）** 是发挥最大作用的技能。在这个项目中，TDD 不是"先写测试再写代码"的教条，而是**设计工具**。当我先写 Mock LLM 的测试时，我被迫思考"Mock LLM 的接口应该是什么样"；当我先写护栏的测试时，我被迫枚举所有需要拦截的危险动作。没有 TDD，这些设计决策会被推迟到实现阶段，导致更频繁的返工。

**Brainstorming** 也有显著作用，但主要体现在项目早期。它帮助我从"测试生成器"这个模糊想法中提炼出"反馈闭环 harness"这个可执行的概念。

## 哪些技能"形式大于实质"

**Writing-plans** 在我的项目中形式大于实质。虽然 PLAN.md 产出了 16 个任务的详细列表，但实际实现时，任务的依赖关系比我预想的更复杂，导致计划需要频繁调整。对于一个时间紧迫的小项目，过于详细的计划反而增加了维护成本。

## TDD 强制在 AI 协作下是阻碍还是放大器

是**放大器**。在 AI 协作场景中，TDD 的价值不仅在于"防止 bug"，更在于**提供客观的完成标准**。当 subagent 或 AI 生成代码时，没有 TDD 的话，"完成"的定义是模糊的——是代码写完了？还是能运行了？还是有测试了？

TDD 给出了明确答案：**红色→绿色**。在看到测试从红变绿之前，任务不算完成。这个标准不需要人来做主观判断。

## Subagent 驱动工作流让智能体能自主运行多久而不偏离主题

在我的体验中，单个 task 的颗粒度最优时，subagent 可以自主运行 15-30 分钟而不偏离。但前提是 task 定义必须非常精确——包括"涉及哪个文件""实现什么接口""验证步骤是什么"。

当 task 定义模糊时（比如"实现测试执行器"），subagent 容易在细节上偏离（比如花费大量时间处理边缘的解析逻辑）。

## 什么样的 task 颗粒度最优

最优颗粒度是：**一个 task 对应一个模块的一个行为**。例如：
- 不是"实现护栏"，而是"护栏拦截 `rm -rf /` 命令"
- 不是"实现反馈"，而是"将 AssertionError 分类为 ASSERTION_FAILURE"

这种颗粒度下，每个 task 有明确的输入输出，测试可以直接验证行为。

## SPEC/PLAN 质量如何影响实现质量

一个具体的例子：SPEC 中对"失败分类"的定义不够精确，只说"分析失败原因并分类"。实现时，我最初只区分了"通过"和"失败"。后来测试运行中发现，语法错误和断言失败需要完全不同的反馈格式——前者需要告诉 LLM "你的代码有语法错误"，后者需要告诉它"你的断言值不对"。

如果 SPEC 在最初就明确列出所有失败类型，这个返工可以避免。

## 最有效的 prompt/context 策略

最有效的策略是**在上下文中持续累积反馈**，而不是在每轮重新开始。当 Agent Loop 的上下文包含"上一轮失败的具体信息"时，LLM（即使是 Mock）的修正方向会更加明确。这验证了"上下文设计是 harness 的核心工程问题"这一命题。

## 凭据与分发迫使我想清楚了什么

凭据管理迫使我想清楚了一个容易忽视的问题：**"API Key 的生命周期"**。Key 不仅需要安全存储，还需要安全的录入、查看、更新、清除流程。最简单的方式（环境变量）其实是最不安全的方式之一（会进入 shell history）。

分发迫使我想清楚另一个问题：**"别人怎么运行我的项目"**。这不仅仅是"给别人一个 GitHub 链接"，而是要考虑目标机器的环境差异、依赖安装、首次运行体验。

## 如果重做我会改变什么

1. **更早地引入 Mock LLM**：我最初想先接入真实 LLM 再考虑测试。实际上应该反过来——先有 Mock LLM，确定接口，再接入真实 LLM。
2. **减少模块数量**：8 个并行模块对于一个时间有限的项目来说太多了。4-5 个模块可能更合适。
3. **提前处理路径问题**：Windows 和 Unix 的路径差异消耗了大量调试时间。应该在项目初期就统一路径处理逻辑。

## 对 Superpowers 方法论的批判

Superpowers 假设了**充裕的时间和完整的团队流程**。它的七步工作流（brainstorming → writing-plans → worktrees → subagent-driven-development → TDD → code-review → finishing）每一步都需要时间和精力。

在我的项目里，最不成立的假设是 **"每个 task 都可以由新鲜 subagent 完成"**。实际上，某些 task（如主循环）需要对其他模块的深入理解，新鲜 subagent 缺乏这种上下文，反而效率更低。

另一个不成立的假设是 **"worktree 并行开发总是有益的"**。当模块间有隐式依赖时（如 path 解析的一致性），并行开发反而导致不一致的实现，需要额外的合并成本。

Superpowers 是一套有用的**流程脚手架**，但它不能替代工程师对"哪些流程值得遵守、哪些可以跳过"的判断。