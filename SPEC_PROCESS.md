# SPEC_PROCESS.md — 规约生成过程记录

## 概述

本项目使用 Superpowers 框架的 brainstorming 技能进行规约设计。以下是关键迭代过程记录。

## Brainstorming 关键节点

### 初始想法

最初的想法非常简单："做一个测试生成器，AI 生成测试用例，自动运行，失败就修正。"

### 智能体追问的关键问题

1. **"测试生成是一个动作还是一个闭环？"**
   - 这个问题促使我将项目定位从"生成工具"转变为"harness"——核心不是生成，而是反馈闭环。

2. **"你怎么区分测试写错了和代码本身有 bug？"**
   - 这让我意识到需要设计"最大迭代轮数"和"失败分类"机制。当达到最大轮数仍然失败时，需要给用户一个明确的诊断报告。

3. **"哪些机制是确定性的代码，哪些只是提示词？"**
   - 这是最关键的问题。它迫使我把护栏、反馈校验器、停机判断都设计为纯函数，而不是写在 prompt 里的要求。

4. **"你的 Mock LLM 怎么设计才能让核心机制离线可测？"**
   - 这个问题帮助我设计了 LLM 抽象层：真实 LLM 和 Mock LLM 实现同一接口，Mock 按预设脚本返回，支持错误注入。

## 三轮关键迭代

### 迭代 1：从"工具"到"harness"

**对话节选**：
> 我：我想做一个测试生成器。
> Agent：你只是想生成测试，还是想构建一个能自我修正的系统？
> 我：有什么区别？
> Agent：如果是前者，你只需要调一次 API。如果是后者，你需要设计主循环、反馈机制、治理机制。

**决策**：定位为 harness 项目，聚焦反馈闭环。

### 迭代 2：失败分类的粒度

**对话节选**：
> Agent：测试失败有很多类型——语法错误、导入错误、断言失败、超时。你打算都按"失败"处理吗？
> 我：有什么区别吗？
> Agent：如果你只告诉 LLM "失败了"，它可能需要 5 轮才能找到问题。如果你告诉它 "这是语法错误，在第 3 行"，它可能 1 轮就修好了。

**决策**：设计了 6 种失败分类（SYNTAX_ERROR、IMPORT_ERROR、ASSERTION_FAILURE、RUNTIME_ERROR、TIMEOUT、ALL_PASSED），每种对应不同的反馈格式。

### 迭代 3：Mock LLM 的必要性

**对话节选**：
> Agent：你的测试怎么运行？每次都要调真实 LLM 吗？
> 我：可以用真实 LLM。
> Agent：但这样测试就不确定了。同一个输入，真实 LLM 每次输出不同。你怎么验证 harness 本身的逻辑是对的？
> 我：那我需要一个假 LLM？
> Agent：是的，一个按脚本返回的 Mock LLM。这样你可以确定性地测试"当 LLM 返回 X 时，harness 应该做 Y"。

**决策**：设计 MockLLM 类，实现与真实 LLM 相同的接口，按预设脚本返回响应。

## AI 提出而我采纳的建议

1. 失败分类机制（6 种类别）
2. Mock LLM 设计
3. 路径穿越检测（护栏的一部分）
4. 测试执行超时设置
5. 连续解析失败自动中止

## AI 提出而我推翻或修正的建议

1. **AI 建议使用 LangChain 的 AgentExecutor** → 我推翻了，因为课程要求不能寄生在现成框架上。
2. **AI 建议加入覆盖率作为反馈信号** → 我推迟了，初版只保留 pass/fail 作为反馈。
3. **AI 建议向 LLM 提供完整函数源码** → 我修正为默认只提供签名和 docstring，防止过拟合。

## 反思

Brainstorming 技能做得好的地方：
- 它能从模糊想法中提炼出"闭环"这个核心概念
- 它追问的"怎么测试"问题直接导向了 Mock LLM 设计
- 它帮助我在写代码前就想清楚了架构

Brainstorming 技能让我不满的地方：
- 有时追问过于发散，偏离了核心目标
- 需要人工不断拉回焦点
- 对于一些显而易见的问题（比如需要 `.gitignore`），追问过多